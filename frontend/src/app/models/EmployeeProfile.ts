export class EmployeeProfile {
  id!: number;
  firstName!: string;
  lastName!: string;
  username!: string;
  email!: string;
  phone!: string;
  profilePicture!: string | null;
}

//same as athlete profile could have used only one profile
//i will just use athlete profile